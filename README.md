# W3 Prospectus-Style Landing Pages (Master Bundle)

Each folder contains a standalone prospectus-style lander (`index.html`) plus a README.

All forms POST to: https://w3.mortgage/api/inquiry.php
Each page sends `domain = window.location.hostname` so the backend routes correctly.

Footer on every page states domain ownership: Ranzo Tech LLC.
